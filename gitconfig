[user]
	name = Maher Sallam
	email = msallam@rocsys.com
[core]
    pager = delta
	editor = code --wait
[credential]
	helper = store
[interactive]
    diffFilter = delta --color-only
[merge]
    conflictstyle = diff3
[diff]
    colorMoved = default

[delta]
    features = side-by-side line-numbers navigate

[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
